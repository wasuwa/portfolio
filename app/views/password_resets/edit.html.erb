<!-- パスワード再設定 -->
<section class="settings">
    <div class="settings__wrapper">
    <div class="settings__icon">
        <%= image_tag "default_icon.jpg", class: "settings__icon--img" %>
    </div>
    <h2 class="settings__wrapper--title">パスワードの再設定</h2>
    <%= form_with(url: password_resets_path, scope: :password_reset, local: true, class: 'settings__form settings__form_margin') do |f| %>
        <%= hidden_field_tag :email, @user.email %>
        <%= f.label :password, "新しいパスワード", class: "settings__form--label" %>
        <%= f.email_field :password, class: "settings__form--input" %>
        <%= f.label :password_confirmation, "新しいパスワードの確認", class: "settings__form--label" %>
        <%= f.email_field :password_confirmation, class: "settings__form--input" %>
        <%= f.submit "パスワードを変更する", class: "settings__form--button" %>
    <% end %>
</section>